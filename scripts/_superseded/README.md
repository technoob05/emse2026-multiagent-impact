

`validate_outputs.py` and `validate_handoff_outputs.py` check tables from the
pre-pivot study (`rq1_event_study.csv`, `rq3_recovery_rates.csv`,
`direct_handoff_quality.csv`). Those tables answer different questions from the
current RQ1 to RQ4 and now live under `outputs/_superseded/`. Retired 2026-08-27.
